We thank the reviewer for the constructive feedback and for recognizing our focus on real-world robotic stow scenarios.

We'd like to first clarify the data setting we target [为什么是clarify data setting？我明明是觉得是clarify target的scenario面临的真实困境]. In real production stow systems, the available data is fundamentally different from clean academic benchmarks: [不要用冒号，把冒号从你的写作里完全去掉] one typically has large-scale before/after snapshots from executed stows [其实你想说的应该是实际的production环境我们常常会有的数据信息才对], but not ground-truth instance masks, not perfect item tracking, and not explicit labels telling us which post-stow region corresponds to the newly inserted item [这些没有的东西是非常specific to我们的方法的，放在这里非常突兀，因为我们明明在说general的production stow环境下没有的东西才对]. ARMBench is representative of exactly this setting [不是说就是representative，实际上我就只找到了这么一个production stow的公开数据集]. As a result, the central difficulty is not how to learn from perfectly curated supervision [这个learn from perfectly supervision其实是在指clean academic dataset对吧？要明确说出来], but how to build an automated pipeline that can mine usable single-step transitions from noisy industrial logs with as little human intervention as possible [这句话how to写的好，应该放到最前面，写东西要能上来就让人看懂]. Our method is designed around this reality: [不要用冒号，把冒号从你的写作里完全去掉] automatically filter obvious noise, automatically validate correspondences when possible, and then test whether the resulting supervision is already sufficient for learning meaningful post-stow dynamics. The empirical results suggest that it is [你怎么能写的这么口语化，我们在做academic的paper rebuttal啊，语言风格应该怎么样你不知道吗？]: despite noisy supervision, FOREST substantially improves over heuristic baselines.

**W1:** The method heavily relies on upstream modules, and the effect of supervisory noise is unclear.  
**A1:** We agree that upstream modules introduce noise [这句话也非常生硬，不能因为字数限制就把句子写的这么生硬啊]. The key point is that this noise cannot be quantified precisely on ARMBench because there is no ground-truth segmentation and no ground-truth item matching to compare against [虽然事实是这样，但是你说的也太生硬了，我们既然写了上面的第二段，就应该用上啊]. Given this constraint, our goal is not to claim noise-free supervision, but to reduce the noise automatically as much as possible and then test whether the learned model is still useful. Concretely, we use algorithmic item matching together with an automated consistency check from Claude-3.7 over the pre/post RGB images. The fact that [这种the fact that...shows that...听起来非常有攻击性] FOREST improves newly inserted item IoU from 0.3632 to 0.7017 in direct insert and from 0.2167 to 0.6166 in sweep insert shows that the proposed architecture is able to extract robust stow dynamics [这里感觉在overclaim，为什么这个结果就说明了"extract robust stow dynamics"？我们明明是在解释upstream modules没有影响到我们的proof-of-concept呀，事实上我们应该在开头就写清楚我们确实有很多upstream modules，它们的noise都会影响下游的这个visual foresight的model的performance，但是首先就像第二段说的，我们的目的不是做noise-free supervision而是在这种真实的数据情况下，仍然做一些可用的模型探索，我们也确实POC了，然后才是noise的影响难以quantify因为受限于数据。这些回答的逻辑你要理清楚，用真正合理的总分、第一第二的结构来写response] from this automatically curated, imperfect supervision.

**W2:** The assumption that instance count change identifies the new item is strong.  
**A2:** We would like to clarify that `N -> N+1` is not a theoretical assumption about the physical world. It is a practical data-filtering heuristic used to identify records that are likely to correspond to clean single-step stows. Since the industrial data does not provide annotations identifying the new item, this count change helps us filter out heavily occluded, ambiguous, or effectively multi-step records before matching. In other words, the role of the count change is to make large-scale automatic curation feasible without introducing massive human labeling effort. [这个回复的内容基本是对的，但是缺少了一个关键，那就是为什么？为什么我要fileter出来clean single-step stows？为什么要filter out heavily occluded, ambiguous, or effectively multi-step records？这些应该在解释"`N -> N+1` is not a theoretical assumption"之前就说清楚。]

**W3:** The motivation for using diffusion models is not clearly demonstrated.  
**A3:** The motivation comes directly from the physical nature of stow. Real stow outcomes are highly stochastic and often multi-modal: [不要用冒号，把冒号从你的写作里完全去掉] an unstable item may tip left or right, settle differently under contact, or trigger different collision chains in existing items. This type of uncertainty is difficult to capture with deterministic placement rules or simple regressors. Diffusion models are attractive here because recent literature suggests that they can serve as world models capable of representing complex future-state distributions and underlying physical interactions [千万注意不要overclaim]. Our architecture is built on this premise for the sparse-snapshot regime. This is also where the gains appear most clearly [为什么比heuristic baseline好就是gains？逻辑要说清楚，那就是heuristic baselines其实就是deterministic placement rules]: on direct-insert surprise cases, N-IoU increases from 0.1034 to 0.5623, precisely where simple heuristics fail.

**Q1:** Please clarify the VLM filtering details.  
**A4:** Claude-3.7 is used as an automated consistency checker to cross-verify the algorithm-based matching result without manual verification as described in Appendix C (lines 652-655). The prompt used is: `You are an assistant to find out the newly added mask in the second image, given two images. Format your response as: "[Analysis]: <your reasoning process>; \n\n [Final Answer]: <mask_index>". The mask_index should be the index of the identified newly added mask, counting from the left.`

**Q2:** Why these six item attributes, and are they sufficient?  
**A5:** These six item attributes are natively provided by ARMBench. They are not intended to be an exhaustive physical ontology. Instead, they provide compact physical cues that are directly relevant to post-stow behavior, such as deformability, rolling tendency, and box-like contact behavior [把property和你这里提到的deformability对应上]. Their practical value is also supported by the ablation study: [不要用冒号，把冒号从你的写作里完全去掉] removing the property token leads to a measurable drop in N-IoU.

---

## Revised Draft

We thank the reviewer for the constructive feedback and for recognizing our focus on real-world robotic stow scenarios.

We would like to first clarify the target scenario and the practical difficulty it brings. Our goal is not to study stow under fully curated academic supervision, but to study production-style robotic stow, where the available data is large-scale yet inherently incomplete. In a real warehouse system, what one commonly has is a large number of executed stows together with before/after observations and operational metadata. What one usually does not have is dense human annotation for every step, perfect tracking across the transition, or labels that directly identify all correspondences needed by the downstream learning pipeline. ARMBench is the only public production-scale stow dataset we found, and it reflects exactly this reality. For this reason, the key question in our paper is not how to learn under noise-free supervision, but how to automatically reduce noise, minimize human intervention, and then test whether the resulting supervision is already sufficient for a useful proof-of-concept model. Our results indicate that this is feasible in practice [这句话有点空洞，要不用细节丰富一下，要不就不要这句话了].

**W1:** The method heavily relies on upstream modules, and the effect of supervisory noise is unclear.  
**A1:** We agree that the upstream modules introduce noise, and that this noise will inevitably affect the downstream visual foresight model. The key point is that this is exactly the practical setting we are targeting rather than an avoidable artifact of the study. In ARMBench, there is no ground-truth segmentation and no ground-truth item matching, so the influence of supervisory noise cannot be quantified precisely against clean labels. Under this constraint, our objective is not to claim noise-free supervision, but to reduce noise as much as possible through automation and then evaluate whether the learned model remains useful. 

Concretely, we first construct matches algorithmically, and then use Claude-3.7 as an automated consistency checker over the pre/post RGB images to filter clearly unreliable cases. This does not make the supervision noise-free, but it makes the pipeline scalable and practically usable. More importantly, the resulting model performance shows that the remaining supervision still carries meaningful signal. FOREST improves the newly inserted item IoU from 0.3632 to 0.7017 in direct insert and from 0.2167 to 0.6166 in sweep insert. In our view, this is the relevant proof-of-concept [this is the relevant proof-of-concept 这句话我没看懂，你想表达什么？]. Even under imperfect, automatically curated supervision that resembles the real deployment setting, the proposed architecture still learns a useful predictive model [最后这句话好像太重复了和前一句话的联系也不清楚，这是和proof-of-concept联系上的吗？].

**W2:** The assumption that instance count change identifies the new item is strong.  
**A2:** We would like to clarify the role of the `N -> N+1` condition. It is not a theoretical assumption about how the physical world behaves. Rather, it is a practical filtering heuristic introduced because the industrial logs do not provide annotations that directly identify the newly inserted item. Before one can train the prediction model, one first needs to mine records that are likely to correspond to clean single-step transitions and are suitable for reliable matching. This is why we filter out cases that are heavily occluded, ambiguous, or effectively multi-step. The purpose of the count change is therefore data curation, not physical modeling. It allows us to build a scalable automatic pipeline without relying on large human annotation efforts, which is exactly the kind of constraint a real production system imposes. [这一段回答写的很好，不过我觉得读起来有点冗长，就是flow很好，但是内容上觉得有些重复的地方，你看看还能怎么修改]

**W3:** The motivation for using diffusion models is not clearly demonstrated.  
**A3:** The motivation comes from the physical nature of stow itself. Real stow outcomes are highly stochastic and often multi-modal. An item may tip in different directions, settle differently under local contact, or trigger different cascaded motions in the existing items. In that setting, deterministic placement rules and heuristic baselines are inherently limited because they collapse many plausible futures into one simple outcome. Diffusion models are attractive because recent work suggests that they can model complex future-state distributions and serve as effective world models for stochastic physical processes. 

Our architecture is built on that motivation and adapted to the sparse-snapshot schema of ARMBench. This is also why the largest gains appear on the difficult cases. On direct-insert surprise cases, N-IoU increases from 0.1034 to 0.5623. In other words, the cases where deterministic heuristics fail most severely are exactly the cases where the diffusion-based model shows the clearest advantage.

**Q1:** Please clarify the VLM filtering details.  
**A4:** No manual verification is involved. Claude-3.7 is used as an automated consistency checker to cross-verify the algorithm-based matching result, as described in Appendix C (lines 652-655). The prompt is `You are an assistant to find out the newly added mask in the second image, given two images. Format your response as: "[Analysis]: <your reasoning process>; \n\n [Final Answer]: <mask_index>". The mask_index should be the index of the identified newly added mask, counting from the left.`.

**Q2:** Why these six item attributes, and are they sufficient?  
**A5:** These six item attributes are natively provided by ARMBench, and we use them as compact physical cues that are directly relevant to post-stow behavior. 
For example, `foldable` corresponds to deformability, `round` relates to rolling tendency, and `square` relates to more box-like contact behavior. They are not intended to form an exhaustive ontology of object physics. Their role is to inject a small amount of practically useful physical information that is already available in the production data. The ablation study supports this design choice, since removing the property token leads to a measurable drop in N-IoU.

---

## Revised Draft V2

We thank the reviewer for the constructive feedback and for recognizing our focus on real-world robotic stow scenarios.

We would like to first clarify the target scenario and the practical difficulty it brings. Our goal is not to study stow under fully curated academic supervision, but to study production-style robotic stow, where the available data is large-scale yet inherently incomplete. In a real warehouse system, what one commonly has is a large number of executed stows together with before/after observations and operational metadata. What one usually does not have is dense human annotation for every step, perfect tracking across the transition, or labels that directly identify all correspondences needed by the downstream learning pipeline.

ARMBench is the only public production-scale stow dataset we found, and it reflects exactly this reality. **Therefore the key question in our paper is not how to learn under noise-free supervision, but how to automatically reduce noise, minimize human intervention, and then test whether the resulting supervision is already sufficient for a useful proof-of-concept model.** Our empirical results support this direction, since FOREST substantially outperforms heuristic baselines even when trained from this automatically curated supervision.

**W1:** The method heavily relies on upstream modules, and the effect of supervisory noise is unclear.  
**A1:** We agree that the upstream modules introduce noise, and that this noise will affect the downstream visual foresight model. The central issue, however, is that this is exactly the practical setting we aim to study rather than an avoidable artifact. In ARMBench, there is no ground-truth segmentation and no ground-truth item matching, so the influence of supervisory noise cannot be quantified precisely against clean labels. Under this constraint, our objective is not to claim noise-free supervision, but to reduce noise as much as possible through automation and then evaluate whether the learned model is still useful.

Concretely, we first construct matches algorithmically, and then use Claude-3.7 as an automated consistency checker over the pre/post RGB images to filter clearly unreliable cases. This does not remove all noise, but it makes the pipeline scalable and practically usable. **The main empirical point is that, after this automatic curation, the resulting supervision is still sufficient to support a strong proof-of-concept model.** FOREST improves the newly inserted item IoU from 0.3632 to 0.7017 in direct insert and from 0.2167 to 0.6166 in sweep insert, which shows that the proposed architecture remains effective even under imperfect industrial supervision.

**W2:** The assumption that instance count change identifies the new item is strong.  
**A2:** We would like to clarify the role of the `N -> N+1` condition. It is not a theoretical assumption about how the physical world behaves. Rather, it is a practical filtering heuristic introduced because the industrial logs do not provide annotations that directly identify the newly inserted item.

Before training the prediction model, one first needs to mine records that are likely to correspond to clean single-step transitions and are suitable for reliable matching. This is why we filter out cases that are heavily occluded, ambiguous, or effectively multi-step. **The purpose of the count change is therefore data curation, not physical modeling.** It allows us to build a scalable automatic pipeline without relying on large human annotation efforts.

**W3:** The motivation for using diffusion models is not clearly demonstrated.  
**A3:** The motivation comes from the physical nature of stow itself. Real stow outcomes are highly stochastic and often multi-modal. An item may tip in different directions, settle differently under local contact, or trigger different cascaded motions in the existing items. In that setting, deterministic placement rules and heuristic baselines are inherently limited because they collapse many plausible futures into one simple outcome.

Diffusion models are attractive because recent work suggests that they can model complex future-state distributions and serve as effective world models for stochastic physical processes. Our architecture is built on that motivation and adapted to the sparse-snapshot regime of ARMBench. This is also why the clearest advantage appears on the difficult cases. On direct-insert surprise cases, N-IoU increases from 0.1034 to 0.5623, precisely where deterministic heuristics fail most severely.

**Q1:** Please clarify the VLM filtering details.  
**A4:** No manual verification is involved. Claude-3.7 is used as an automated consistency checker to cross-verify the algorithm-based matching result, as described in Appendix C (lines 652-655). The prompt is `You are an assistant to find out the newly added mask in the second image, given two images. Format your response as: "[Analysis]: <your reasoning process>; \n\n [Final Answer]: <mask_index>". The mask_index should be the index of the identified newly added mask, counting from the left.`

**Q2:** Why these six item attributes, and are they sufficient?  
**A5:** These six item attributes are natively provided by ARMBench, and we use them as compact physical cues that are directly relevant to post-stow behavior. For example, `foldable` corresponds to deformability, `round` relates to rolling tendency, and `square` relates to more box-like contact behavior.

They are not intended to form an exhaustive ontology of object physics. Instead, they provide a small amount of practically useful physical information that is already available in the production data. The ablation study supports this design choice, since removing the property token leads to a measurable drop in N-IoU.

--
## Final

We thank the reviewer for the constructive feedback and for recognizing our focus on real-world robotic stow scenarios.

We would like to first clarify the target scenario and the practical difficulty it brings. Our goal is to study production-style robotic stow, where the available data is large-scale yet inherently incomplete. In a real warehouse system, we commonly have a large number of executed stows together with before/after observations and operational metadata. What one usually does not have is fully curated supervision such as dense human annotation for every step needed by the downstream learning pipeline.

ARMBench is the representative public production-scale stow dataset, and it reflects exactly this reality. **Therefore the key question in our paper is not how to learn under noise-free supervision, but how to automatically reduce noise, minimize human intervention, and then test whether the resulting supervision is already sufficient for a useful proof-of-concept model.**

**W1:** The method heavily relies on upstream modules, and the effect of supervisory noise is unclear.  
**A1:** We agree that the upstream modules introduce noise, and that this noise will affect the downstream visual foresight model. However, in ARMBench, there is no ground-truth segmentation and no ground-truth item matching, so the influence of supervisory noise cannot be quantified precisely against clean labels. Under this constraint, our objective is not to claim noise-free supervision, but to reduce noise as much as possible through automation and then evaluate whether the learned model is still useful.

Therefore we first construct matches algorithmically, and then use Claude-3.7 as an automated consistency checker to filter clearly unreliable cases. This does not remove all noise, but it makes the pipeline scalable and practically usable. **The main empirical point is that, after this automatic curation, the resulting supervision is still sufficient to support a strong proof-of-concept model.** FOREST improves the newly inserted item IoU from 0.3632 to 0.7017 in direct insert and from 0.2167 to 0.6166 in sweep insert, which shows that the proposed architecture remains effective even under imperfect industrial supervision.

**W2:** The assumption that instance count change identifies the new item is strong.  
**A2:** We would like to clarify that the instance count change is not a theoretical assumption about how the physical world behaves. Instead, it is a practical filtering heuristic to mine records that are likely to correspond to clean single-step transitions and are suitable for reliable matching. **The purpose of the count change is therefore data curation, not physical modeling.** It allows us to build a scalable automatic pipeline without relying on large human annotation efforts.

**W3:** The motivation for using diffusion models is not clearly demonstrated.  
**A3:** The motivation comes from the physical nature of the stow itself. Real stow outcomes are highly stochastic and often multi-modal. An item may tip in different directions, settle differently under local contact, or trigger different cascaded motions in the existing items. In that setting, deterministic models are inherently limited because they collapse many plausible futures into one simple outcome. Diffusion models are attractive because recent work suggests that they can model complex distributions and serve as effective world models for stochastic physical processes. Our architecture is built on that motivation and adapted to the sparse-snapshot inputs of ARMBench. 

**Q1:** Please clarify the VLM filtering details.  
**A4:** In VLM filtering, Claude-3.7 is used as an automated consistency checker to cross-verify the algorithm-based matching result, as described in Appendix C (lines 652-655). The prompt is `You are an assistant to find out the newly added mask in the second image, given two images. Format your response as: "[Analysis]: <your reasoning process>; \n\n [Final Answer]: <mask_index>". The mask_index should be the index of the identified newly added mask, counting from the left.`

**Q2:** Why these six item attributes, and are they sufficient?  
**A5:** These six item attributes are natively provided by ARMBench, and we use them as compact physical cues that are directly relevant to post-stow behavior. For example, `foldable` corresponds to deformability, `round` relates to rolling tendency, and `square` relates to more box-like contact behavior. They are not intended to form an exhaustive ontology of object physics. Instead, they provide a small amount of practically useful physical information that is already available in the production data. The ablation study supports this design choice, since removing the property token leads to a measurable drop in N-IoU.
